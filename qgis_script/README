Depencies:
-gdal


Installation instructions:

In order to use the QGIS runner script the executable needs to be placed in directory on your search path. For example if executable is located at ~/bin run following comand to add ~/bin to your search path:

export PATH="/home/$USER/bin:$PATH"

After this you can run lcp search tool with command lcpc. See lcpc -h for details.

To use QGIS runner script open processing toolbox from QGIS and add the lcp_runner.py script using "Tools"->"Add script from file"

